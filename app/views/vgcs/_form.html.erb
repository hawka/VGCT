<%= form_for(@vgc) do |f| %>
  <% if @vgc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vgc.errors.count, "error") %> prohibited this vgc from being saved:</h2>

      <ul>
      <% @vgc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :allegiance %><br />
    <%= f.text_field :allegiance %>
  </div>
  <div class="field">
    <%= f.label :server %><br />
    <%= f.text_field :server %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
